# Frontend

Vite + React + TypeScript + Tailwind. See the [repo root README](../README.md) for full setup (Supabase project, migrations, edge function deploy, env vars).

```bash
npm install
npm run dev
```
